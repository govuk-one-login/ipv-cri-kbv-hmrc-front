{% extends "base-form.njk" %}
{% from "hmpo-text/macro.njk" import hmpoText %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/inset-text/macro.njk" import govukInsetText %}

{% set gtmJourney = "kbv-hmrc - middle" %}

{% set hmpoTitle = question.key %}

{% block mainContent %}
  <h1 class="govuk-heading-l"> {{ translate("pages.rti-payslip-national-insurance.title") }} </h1>

  {{ govukInsetText({
    text: translate("pages.rti-payslip-national-insurance.inset")
  }) }}

  {{ hmpoHtml(translate("pages.rti-payslip-national-insurance.content")) }}

  {% call hmpoForm(ctx) %}

    {{ govukInput({
      id: "question",
      name: "question",
      label: {
        text: question.label,
        classes: "govuk-label--m"
      },
      hint: {
        text: question.hint
      },
      prefix: {
        text: "£"
      },
      classes: "govuk-input--width-5"
    }) }}

    {{ hmpoSubmit(ctx, {id: "continue", text: translate("buttons.next")}) }}
  {% endcall %}

  {{ hmpoHtml(translate("pages.rti-payslip-national-insurance.abandon")) }}

{% endblock %}
